#pragma once

/**
 * @file Requests.hpp
 * @brief Convenience header — includes all service request types.
 * @author Nikola Radovanovic
 *
 * Include this header to pull in every request class in one line.
 * Prefer including individual request headers in headers that only
 * need one type — include this only in translation units that use
 * the full set.
 */

#include <cppenv/server/requests/BundleId.hpp>
